### Learn the algorithm that runs in O(n) time and O(n) space complexity 

# Manacher's Algorithm: Longest Palindromic Substring

## Key Points
- O(n) time complexity for palindrome detection
- Linear-time string processing
- Uses auxiliary array to track palindrome centers
- Avoids redundant character comparisons

## Implementation Notes
- Transform input string 
- Expand palindrome around centers
- Track longest palindrome length
